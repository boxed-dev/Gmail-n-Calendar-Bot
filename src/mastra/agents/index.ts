import { Agent } from '@mastra/core/agent'
import { googleCalendarAgent } from './googleCalendarAgent'
import { gmailAgent } from './gmailAgent'
import { emailCalendarAgent } from './emailCalendarAgent'

// Export the agents
export { googleCalendarAgent, gmailAgent, emailCalendarAgent }
